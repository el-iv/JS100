if (true) {
  let myValue = 20;
}

console.log(myValue);

/*
  let variables are blocked scoped. This means that when we declare the variable
  myValue within a block on line 2, that variable is not accessible outside of
  the block on line 5. An error is raised.

*/
